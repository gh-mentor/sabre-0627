# this script contains git commands to stage, commit, and push changes to the 'main' branch remote repository.

# Stage all changes
git add .

# Commit changes
git commit -m "update"

# Push changes to remote repository
git push origin main